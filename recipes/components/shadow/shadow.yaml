---
name: 'shadow'

component_substitution_list: ['<userinput>passwd root',
                              '<userinput remap="post">echo "root:{p}" | chpasswd',
                              '{p}',
                              'config.ROOT_PASSWD']
