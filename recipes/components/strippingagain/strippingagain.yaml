
name: 'strippingagain'
base_component: 'SystemConfigurationComponent'

# According to the link below, not all files can be stripped, so we ignore those errors
# http://archives.linuxfromscratch.org/mail-archives/lfs-support/2002-July/008317.html
# component_substitution_list: ['/tools/lib/*',
#                     '/tools/lib/* || true',

#                     '/tools/{,s}bin/*',
#                     '/tools/{,s}bin/* || true']

disable_commands_list: ['logout']